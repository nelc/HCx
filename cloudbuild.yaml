steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/hcx-staging:$COMMIT_SHA', '.']
  
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/hcx-staging:$COMMIT_SHA']
  
  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'hcx-staging'
      - '--image'
      - 'gcr.io/$PROJECT_ID/hcx-staging:$COMMIT_SHA'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--add-cloudsql-instances'
      - '${_INSTANCE_CONNECTION_NAME}'
      - '--set-env-vars'
      - 'NODE_ENV=staging,INSTANCE_CONNECTION_NAME=${_INSTANCE_CONNECTION_NAME}'
      - '--set-secrets'
      - 'DB_USER=hcx-db-user:latest,DB_PASS=hcx-db-pass:latest,DB_NAME=hcx-db-name:latest,JWT_SECRET=hcx-jwt-secret:latest,OPENAI_API_KEY=hcx-openai-key:latest'

substitutions:
  _REGION: 'me-central1'
  # Format: PROJECT_ID:REGION:INSTANCE_NAME (e.g., my-project:us-central1:hcx-db)
  _INSTANCE_CONNECTION_NAME: '${PROJECT_ID}:me-central2:hcx-db'

images:
  - 'gcr.io/$PROJECT_ID/hcx-staging:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY

