PORT=3001

# Cloud SQL Configuration (for Google Cloud Run deployment)
INSTANCE_CONNECTION_NAME=app-sandbox-factory:me-central2:app-factory-db
DB_USER=Hrxuser
DB_PASS=lkdfvsdSDD565?
DB_NAME=hrx

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Environment
NODE_ENV=development

# JWT Secret (generate a random string for production)
JWT_SECRET=your-secret-key-here

# NOTE: Do NOT use DATABASE_URL when using Cloud SQL with INSTANCE_CONNECTION_NAME
# The two formats are mutually exclusive

